# CP-0.2 Feature Matrix

| Feature | Status | Notes |
| :--- | :--- | :--- |
| Firebase authentication | UNKNOWN | Code exists (`signInWithPopup` in `src/App.tsx`), but runtime behavior cannot be verified automatically. |
| User initialization | UNKNOWN | Code exists (`onAuthStateChanged` in `src/App.tsx`), but runtime behavior cannot be verified automatically. |
| Individual game state | UNKNOWN | Code exists (Firestore `users/{userId}` collection logic), but runtime behavior cannot be verified automatically. |
| Isometric environment | UNKNOWN | Code exists (`src/components/game/IsometricGrid.tsx`), but runtime behavior cannot be verified automatically. |
| Buildings | UNKNOWN | Code exists, but runtime behavior cannot be verified automatically. |
| Resources | UNKNOWN | Code exists, but runtime behavior cannot be verified automatically. |
| Upgrades | UNKNOWN | Code exists, but runtime behavior cannot be verified automatically. |
| Builder system | UNKNOWN | Code exists, but runtime behavior cannot be verified automatically. |
| Missions | UNKNOWN | Code exists (`DailyMissions.tsx`), but runtime behavior cannot be verified automatically. |
| Clan | UNKNOWN | Code exists (`ClanPanel.tsx`), but runtime behavior cannot be verified automatically. |
| Fork interaction | UNKNOWN | Code exists (`ForkClanSelector.tsx`), but runtime behavior cannot be verified automatically. |
| Simulated infiltration | UNKNOWN | Code exists (`AttackButton.tsx`), but runtime behavior cannot be verified automatically. |
| Orchestration/status interface | UNKNOWN | Code exists (`OrchestrationPanel.tsx`), but runtime behavior cannot be verified automatically. |
